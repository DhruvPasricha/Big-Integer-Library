#include <iostream>
using namespace std;

class bigint
{

    // data members [start]
private:
    string num;
    // data members [end]

public:
    // constructors [start]
    bigint() // default constructor
    {
        this->num = "";
    }
    bigint(string num) // copy constructor
    {
        this->num = num;
    }
    // constructors [end]

    // input/output operators (>> <<) [start]
    // input/output operators (>> <<) [end]
    
    
    // arithematic operators (+ - * / %) [start]
    // arithematic operators (+ - * / %) [end]


    // Math functions (gcd/lcm fibonacci factorial)[start]
    // Math functions (gcd/lcm fibonacci factorial)[start]
};
